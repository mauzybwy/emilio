;;;;;                            ;;
;;;;  Helm Mode Configuration   ;;;
;;;            - mauzybwy -    ;;;;
;;                            ;;;;;

(use-package helm
  ;; :bind (("M-x"     . helm-M-x)
  ;;        ("C-x C-b" . helm-buffers-list)
  ;;        ("C-x b"   . helm-mini)
  ;;        ("C-x C-f" . helm-find-files)
  ;;        ([S-f10]   . helm-recentf))
  
  ;; :init
  ;; (require 'helm)
  )
